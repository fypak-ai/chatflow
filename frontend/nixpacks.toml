# Frontend — Nixpacks detecta Node pelo .node-version
# Sem forçar nixPkgs para evitar conflitos de versão

[phases.install]
  cmds = ["npm ci"]

[phases.build]
  cmds = ["npm run build"]

[start]
  cmd = "node server.js"
